<% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
<% end %>

<section class="show_page">
	<p class="show_title"><strong>Titile:</strong><%= @book.title %></p>

	<p class="show_body"><strong>Body:</strong><%= @book.body %></p>

	<%= link_to "Edit", edit_book_path(@book), class: "edit_#{@book.id}" %>
	<span>|</span>
	<%= link_to "Back", books_path, class: "back" %>
</section>
